# 작업 계획: {기능/변경명}

- 작성일: {YYYY-MM-DD}
- 출처: `02-spec.md`
- 브랜치: {branch}

## 영향 범위

수정 대상은 실제로 확인한 파일만 적는다. 신규 파일은 배치 근거를 함께 적는다.

| 파일 | 구분 | 내용 | 배치 근거 |
| --- | --- | --- | --- |
| `src/renderer/src/features/{기능}/components/{Name}.tsx` | 신규 | {무엇을 담는지} | architecture-directory.md — 한 기능에서만 사용 |
| `src/main/ipc/{도메인}.ts` | 수정 | {무엇을 바꾸는지} | - |

## 작업 분해

각 항목은 커밋 하나에 대응한다. 위에서 아래 순서로 수행한다.

### 1. {작업 제목}

- 커밋 메시지: `{type}({티켓 ID}): {제목}`
- 대응 SPEC: SPEC-001
- 대상 파일: `{경로}`
- 완료 조건: {무엇을 보면 끝났다고 판단하는가 — 관찰 가능한 형태로}

### 2. {작업 제목}

- 커밋 메시지: `{type}({티켓 ID}): {제목}`
- 대응 SPEC: SPEC-002, SPEC-003
- 대상 파일: `{경로}`
- 완료 조건: {…}
- 선행 조건: {앞 작업에 의존한다면 그 이유 — 없으면 생략}

## 테스트 계획

| 대상 | 종류 | 확인 내용 | 대응 SPEC |
| --- | --- | --- | --- |
| `{경로}` | 단위 | {무엇을 검증하는가} | SPEC-001 |
| `{경로}` | 컴포넌트 | {무엇을 검증하는가} | SPEC-002 |

테스트하지 않는 것:

- {대상} — {이유. test-convention.md의 기준을 근거로}

## SPEC 커버리지

모든 SPEC이 작업에 반영됐는지 역방향으로 확인한다. 비어 있는 행이 있으면 누락이다.

| SPEC | 대응 작업 |
| --- | --- |
| SPEC-001 | 1 |
| SPEC-002 | 2 |

## 리스크 및 후속 작업

- {구현 중 막힐 수 있는 지점, 이번 범위에서 빠지는 후속 작업 — 없으면 "없음"}
